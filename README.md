# ChainMosaic
ChainMosaic is a generative on-chain identity project on the Base network. Every wallet address produces a unique, permanent art pattern derived from its transaction hash DNA. No two mosaics are alike — each is a cryptographic reflection of its wallet’s life on-chain.
